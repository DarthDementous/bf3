// vim: set syntax=c :
	    
chrRef lando    = chrRefFromName("lando");

chrSetPatrolState(lando);

beginInGameCutSceneIgnorePerceptsAndDamage(lando);

chrPlayAnimSlot(lando, "AN_hm_ter_lp", 1, true);

propSetFrozen(lando, true);

