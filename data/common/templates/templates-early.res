// vim: set syntax=c:

/*
    This is for templates that need to be parsed before the project specific early templates.
    The order should be like this: common-early, project-early, common, project.
*/

#include "data/common/templates/fieldmeta.res"
#include "data/common/templates/core-early.res"

